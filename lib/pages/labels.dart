import 'package:flutter/material.dart';

class Labels extends StatelessWidget {
  final String title;
  final String text;
  // final Color color;
  // final Color bordercolor;

  const Labels({required Key? key, required this.title, required this.text})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
        //child: Labels(title: "teste"),
        );
  }
}
